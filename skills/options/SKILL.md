# Options

Treat Indian derivatives as a separate provider boundary. Do not port US expiry, contract, option-chain or settlement assumptions. Use Marked for the underlying company/security identity and use a derivatives provider only when it explicitly supports the requested NSE/BSE contract. Label stale, unavailable and external data.
